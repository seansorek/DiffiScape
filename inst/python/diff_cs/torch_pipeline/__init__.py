"""PyTorch resistance-pipeline package: differentiable circuit solving,
resistance nets, MAP optimisation, and Bayesian samplers.

This package is re-exported wholesale by the sibling ``05_torch_pipeline.py``
shim, which is the module R/torch_bridge.R actually loads (by exact path) via
reticulate. See that file's module docstring for the full re-export list.
"""
