% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/torch_pipeline.R
\name{run_bayesian_sampling_hmc}
\alias{run_bayesian_sampling_hmc}
\title{HMC / NUTS posterior sampling from a trained torch model}
\usage{
run_bayesian_sampling_hmc(
  basis_stack,
  obs_points,
  model_dir,
  n_samples = 1000L,
  warmup = 1000L,
  max_treedepth = 10L,
  target_accept = 0.8,
  init_step_size = NULL,
  adapt_mass_matrix = TRUE,
  fix_smoothing = TRUE,
  fix_intensity = FALSE,
  solver = "global_absorption",
  absorption = 0.002,
  source_spacing = 1L,
  source_from_resistance = TRUE,
  reg_mean = 1,
  log_R_baseline = 3,
  penalty_scale = 5,
  n_knots = 10L,
  spline_degree = 3L,
  include_interactions = FALSE,
  lambda_min = 0.1,
  grad_clip = 100,
  cg_tol = 1e-06,
  seed = 42L,
  verbose = TRUE,
  output_dir = NULL,
  device = "auto"
)
}
\arguments{
\item{basis_stack}{\link[terra:SpatRaster-class]{terra::SpatRaster} of basis layers (must match the
stack used for the MAP run).}

\item{obs_points}{Data.frame with \verb{x, y} columns.}

\item{model_dir}{Character; directory containing \code{resistance_nn.pt}
(the MAP checkpoint produced by \code{\link[=run_torch_pipeline]{run_torch_pipeline()}}).}

\item{warmup}{Integer; warm-up iterations (adapts step size / mass).}

\item{max_treedepth}{Integer; NUTS max tree depth.}

\item{target_accept}{Numeric; MALA target acceptance.}

\item{init_step_size}{Numeric or \code{NULL} for auto.}

\item{adapt_mass_matrix}{Logical.}

\item{fix_smoothing, fix_intensity}{Logical; freeze blocks at MAP.}

\item{solver, absorption, source_spacing, source_from_resistance}{Solver
knobs (must match the MAP run).}

\item{reg_mean, log_R_baseline, penalty_scale}{Prior knobs.}

\item{n_knots, spline_degree, include_interactions}{Spline model knobs.}

\item{lambda_min, grad_clip, cg_tol}{Numerical settings.}

\item{seed}{Integer.}

\item{verbose}{Logical.}

\item{output_dir}{Character; defaults to \code{model_dir}.}

\item{device}{Character; \code{"auto"}, \code{"cuda"}, or \code{"cpu"}.}
}
\value{
A list with posterior samples, \code{summary}, diagnostics
(\code{n_divergences}, etc.), and \code{elapsed_time}.
}
\description{
Runs the No-U-Turn Sampler over the joint posterior, starting from the
MAP checkpoint produced by \code{\link[=run_torch_pipeline]{run_torch_pipeline()}}.
}
