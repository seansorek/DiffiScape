% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/torch_pipeline.R
\name{run_torch_pipeline}
\alias{run_torch_pipeline}
\title{Optimise resistance with a PyTorch neural network}
\usage{
run_torch_pipeline(
  basis_stack,
  obs_points,
  hidden_dim = 32L,
  n_hidden_layers = 2L,
  lr = 0.01,
  weight_decay = 1e-04,
  n_epochs = 300L,
  patience = 30L,
  grad_clip = 10,
  cg_tol = 1e-06,
  source_spacing = 5L,
  source_from_resistance = TRUE,
  reg_mean = 1,
  reg_var = 0.1,
  target_logR_var = 1,
  reg_skip = 0.1,
  log_R_baseline = 3,
  warm_start_theta = NULL,
  output_dir = "output/torch",
  seed = 42L,
  verbose = TRUE,
  solver = "diff_omniscape",
  radius = 15L,
  block_size = 10L,
  focal_fraction = 0.5,
  absorption = 0.01,
  device = "auto",
  use_conv = FALSE,
  n_conv_layers = 3L,
  conv_channels = 16L,
  conv_kernel_size = 3L,
  dropout = 0,
  use_dilated = TRUE,
  intensity_hidden = 0L,
  warmup_epochs = 10L,
  absorption_schedule = NULL,
  model_type = "mlp",
  n_knots = 10L,
  spline_degree = 3L,
  include_interactions = TRUE,
  penalty_scale = 1,
  lambda_init_marginal = 0,
  lambda_init_interaction = 2,
  lambda_min = 0,
  compute_uq = FALSE,
  uq_block_only = TRUE,
  intensity_spline = FALSE,
  intensity_n_knots = 5L,
  intensity_degree = 3L,
  lambda_init_intensity = 2,
  intensity_log1p_max = 10
)
}
\arguments{
\item{basis_stack}{A \link[terra:SpatRaster-class]{terra::SpatRaster} with K covariate layers.}

\item{obs_points}{Data.frame with \verb{x, y} columns (projected coords).}

\item{hidden_dim}{Integer; MLP hidden width.}

\item{n_hidden_layers}{Integer; number of MLP hidden layers.}

\item{lr}{Numeric; Adam learning rate.}

\item{weight_decay}{Numeric; L2 weight decay.}

\item{n_epochs}{Integer; max training epochs.}

\item{patience}{Integer; early-stopping patience.}

\item{grad_clip}{Numeric; gradient-norm clip.}

\item{cg_tol}{Numeric; CG solver tolerance.}

\item{source_spacing}{Integer; circuit source lattice spacing.}

\item{source_from_resistance}{Logical; weight sources by resistance.}

\item{reg_mean}{Numeric; weight on \verb{(mean(log R) - log_R_baseline)^2}.}

\item{reg_var}{Numeric; weight on a variance hinge penalty in \verb{log R}.}

\item{target_logR_var}{Numeric; activation threshold for the var hinge.}

\item{reg_skip}{Numeric; weight on a skip-connection magnitude penalty.}

\item{log_R_baseline}{Numeric; target mean of \verb{log R}.}

\item{warm_start_theta}{Optional numeric vector of log-linear params
\verb{(r_0, z_1, ..., z_K)} to warm-start from.}

\item{output_dir}{Character; directory for saved rasters and
checkpoints.}

\item{seed}{Integer; RNG seed.}

\item{verbose}{Logical.}

\item{solver}{Character; \code{"diff_omniscape"}, \code{"global"}, or
\code{"global_absorption"}.}

\item{radius}{Integer; Omniscape focal radius.}

\item{block_size}{Integer; Omniscape focal block size.}

\item{focal_fraction}{Numeric; stochastic focal sampling fraction.}

\item{absorption}{Numeric; absorption rate for the global solver.}

\item{device}{Character; \code{"auto"}, \code{"cuda"}, or \code{"cpu"}.}

\item{use_conv}{Logical; enable ConvResistanceNet.}

\item{n_conv_layers}{Integer; Conv2d residual blocks.}

\item{conv_channels}{Integer; conv feature channels.}

\item{conv_kernel_size}{Integer; conv kernel size.}

\item{dropout}{Numeric; dropout rate in the MLP head.}

\item{use_dilated}{Logical; dilated convolutions.}

\item{intensity_hidden}{Integer; learned intensity MLP width
(\code{0} = parametric).}

\item{warmup_epochs}{Integer; linear LR warm-up epochs.}

\item{absorption_schedule}{Optional numeric vector for an absorption
curriculum.}

\item{model_type}{Character; \code{"mlp"} or \code{"spline_gam"}.}

\item{n_knots}{Integer; spline knots per covariate.}

\item{spline_degree}{Integer; B-spline degree.}

\item{include_interactions}{Logical; tensor-product interactions.}

\item{penalty_scale}{Numeric; smoothing-penalty multiplier.}

\item{lambda_init_marginal}{Numeric; init marginal smoothing param.}

\item{lambda_init_interaction}{Numeric; init interaction smoothing.}

\item{lambda_min}{Numeric; floor on smoothing parameters.}

\item{compute_uq}{Logical; compute partial-effect uncertainty.}

\item{uq_block_only}{Logical; restrict UQ to spline blocks.}

\item{intensity_spline}{Logical; spline transform on connectivity in
intensity model.}

\item{intensity_n_knots}{Integer; spline knots for the intensity term.}

\item{intensity_degree}{Integer; B-spline degree for the intensity term.}

\item{lambda_init_intensity}{Numeric; init smoothing for the intensity
spline.}

\item{intensity_log1p_max}{Numeric; clamp on \code{log1p(connectivity)}.}
}
\value{
A list with \code{best_params}, \code{best_loglik}, \code{alpha}, \code{gamma},
\code{resistance_raster}, \code{connectivity_raster}, \code{intensity_raster},
\code{loss_history}, \code{best_epoch}, \code{n_params}, \code{n_epochs_run},
\code{total_time}, \code{method}, \code{distribution}, \code{effective_loglinear},
\code{model_type}, plus the diagnostics returned by the Python optimiser.
}
\description{
Trains a neural-network resistance surface (MLP / Conv / Spline-GAM) by
backpropagating through a differentiable circuit solver, maximising a
Poisson point-process log-likelihood with smoothness regularisation.
}
\details{
Wraps \code{run_torch_optimization()} in the bundled Python module
\verb{diff_cs/05_torch_pipeline.py}. Requires \code{\link[=ds_torch_setup]{ds_torch_setup()}} to have been
called.
}
