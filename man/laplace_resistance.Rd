% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{laplace_resistance}
\alias{laplace_resistance}
\title{Laplace approximation at the optimised resistance parameters}
\usage{
laplace_resistance(
  opt_result,
  basis_stack = NULL,
  obs_points = NULL,
  refit = TRUE,
  step = 0.001,
  omniscape_settings = list(),
  intensity_config = default_intensity_config(),
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  available_points = NULL,
  available_covariates = NULL,
  link = link_exp(),
  family = NULL
)
}
\arguments{
\item{opt_result}{Result from \code{\link[=optimize_resistance]{optimize_resistance()}} or
\code{\link[=optimize_resistance_gradient]{optimize_resistance_gradient()}}.}

\item{basis_stack}{A \link[terra:SpatRaster]{terra::SpatRaster} of basis functions (required
when \code{refit = TRUE} or when no surrogate is available).}

\item{obs_points}{Data.frame with \verb{x, y} (required when \code{refit = TRUE}).}

\item{refit}{Logical; when \code{TRUE} (the default) the Hessian is computed
on the true PPP log-likelihood via the JAX solver, which requires
\code{basis_stack} and \code{obs_points}.  When both of those are \code{NULL} and a
GP surrogate is available, a warning is emitted and the function falls
back to the surrogate-mean Hessian automatically so that existing
single-argument calls remain valid.  Set \code{refit = FALSE} explicitly to
suppress the warning and force the surrogate path.  Always set to
\code{TRUE} automatically when \code{opt_result} has no \verb{$surrogate} (i.e., from
\code{\link[=optimize_resistance_gradient]{optimize_resistance_gradient()}}).}

\item{step}{Finite-difference step size for Hessian computation.}

\item{omniscape_settings}{Named list of solver overrides: \code{radius}
(default \code{13L}) and \code{block_size} (default \code{5L}). Used when \code{refit = TRUE}.}

\item{intensity_config}{Optional \code{\link[=default_intensity_config]{default_intensity_config()}} list.}

\item{covariates_obs}{Named list of covariate vectors.}

\item{covariates_rasters}{Named list of covariate rasters.}

\item{residualise}{Logical.}

\item{available_points}{Optional data.frame with \verb{x, y} columns of
available/background locations for selection function families.  When
supplied, bypasses raster quadrature and uses these locations with unit
weights instead.  \code{NULL} (default) uses standard area-weighted raster
integration.}

\item{available_covariates}{Named list of covariate vectors at
\code{available_points} locations.  Required when \code{available_points} is
supplied and the intensity model includes covariates.}

\item{link}{A \link{resistance_link} object (default \code{\link[=link_exp]{link_exp()}}).}

\item{family}{An \link{intensity_family} object, or \code{NULL}.}
}
\value{
A list with \code{mode}, \code{covariance}, \code{precision}, \code{std_error}.
}
\description{
Computes a Gaussian approximation to the marginal posterior of the
resistance parameters by numerically differentiating the true PPP
log-likelihood at the MAP estimate (default).  When \code{basis_stack} and
\code{obs_points} are not supplied, the function falls back to differentiating
the GP surrogate's predicted-mean surface and emits a warning.
}
