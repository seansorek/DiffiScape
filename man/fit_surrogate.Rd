% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer.R
\name{fit_surrogate}
\alias{fit_surrogate}
\title{Fit a surrogate regression model for the outer optimisation loop}
\usage{
fit_surrogate(X, y, type = c("gp", "rf"), config = list())
}
\arguments{
\item{X}{Numeric matrix or data.frame of evaluated parameter vectors
(one row per evaluation).}

\item{y}{Numeric vector of objective values (one per row of \code{X}).}

\item{type}{Character; \code{"gp"} (default, \code{\link[DiceKriging:km]{DiceKriging::km()}} with a
Matern 5/2 kernel) or \code{"rf"} (Random Forest via \code{\link[ranger:ranger]{ranger::ranger()}},
using jackknife-after-bootstrap variance estimates as the predictive
SD needed for acquisition). Defaults to \code{"gp"} so existing callers of
\code{\link[=optimize_resistance]{optimize_resistance()}} are unaffected.}

\item{config}{Named list of extra arguments forwarded to the underlying
constructor (\code{DiceKriging::km()} for \code{"gp"}, \code{ranger::ranger()} for
\code{"rf"}).}
}
\value{
An object of class \code{"ds_surrogate"}: a list with \code{type}, \code{model}
(the fitted backend object), and \code{predictor_names}.
}
\description{
Factory function that fits either a Gaussian Process (\code{"gp"}, the
default and historical behaviour) or a Random Forest (\code{"rf"}) regression
surrogate of the (expensive) outer-loop objective, and wraps it in a
common \code{"ds_surrogate"} object so both backends can be used
interchangeably by the Thompson Sampling / Expected Improvement
acquisition functions and by \code{\link[=loo_cv_surrogate]{loo_cv_surrogate()}} / \code{\link[=laplace_resistance]{laplace_resistance()}}.
}
\seealso{
\code{\link[=predict_surrogate]{predict_surrogate()}}, \code{\link[=default_optimizer_config]{default_optimizer_config()}}
}
