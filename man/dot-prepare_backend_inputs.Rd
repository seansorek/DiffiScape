% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.prepare_backend_inputs}
\alias{.prepare_backend_inputs}
\title{Prepare inputs for JAX/PyTorch-backed sampling and optimisation}
\usage{
.prepare_backend_inputs(basis_stack, obs_points)
}
\arguments{
\item{basis_stack}{A \link[terra:SpatRaster]{terra::SpatRaster} with K covariate layers.}

\item{obs_points}{Data.frame with \verb{x, y} columns (projected coords).}
}
\value{
A named list with:
\describe{
\item{basis_np}{numpy array, shape (n_valid, K).}
\item{obs_np}{numpy array of observation counts per valid cell.}
\item{vmask_np}{numpy boolean mask, shape (n_rows * n_cols,).}
\item{valid_mask}{Logical vector (R side).}
\item{n_rows, n_cols}{Integer grid dimensions.}
\item{cell_area}{Numeric cell area in map units squared.}
\item{n_valid}{Number of valid (non-NA) cells.}
\item{n_obs}{Total observation count.}
}
}
\description{
Converts a SpatRaster basis stack and observation coordinates into numpy
arrays suitable for the bundled Python modules used by both the JAX
bridge (\code{\link[=ds_jax_sample_nuts]{ds_jax_sample_nuts()}}, \code{\link[=ds_jax_sample_advi]{ds_jax_sample_advi()}}) and the PyTorch
pipeline (\code{\link[=run_torch_pipeline]{run_torch_pipeline()}}, \code{\link[=run_bayesian_sampling]{run_bayesian_sampling()}},
\code{\link[=run_bayesian_sampling_hmc]{run_bayesian_sampling_hmc()}}, \code{\link[=run_advi]{run_advi()}}).  Shared by every entry
point that talks to either backend, so both treat data preparation
identically.
}
\keyword{internal}
