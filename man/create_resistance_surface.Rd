% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resistance.R
\name{create_resistance_surface}
\alias{create_resistance_surface}
\title{Create a resistance surface from parameters and basis functions}
\usage{
create_resistance_surface(
  params,
  basis_stack,
  R_min = 1,
  R_max = 5000,
  return_log = FALSE,
  link = link_exp()
)
}
\arguments{
\item{params}{Named numeric vector \strong{or} list with elements
\verb{r_0, z_1, z_2, ..., z_K} where \emph{K} = number of basis layers.
The first element is the intercept; remaining elements are
coefficients for each basis function.}

\item{basis_stack}{A \link[terra:SpatRaster]{terra::SpatRaster} with \emph{K} layers (from
\code{\link[=create_basis_stack]{create_basis_stack()}}).}

\item{R_min, R_max}{Numeric.
Hard floor / ceiling applied after the link (default 1 / 5000).}

\item{return_log}{If \code{TRUE}, return the linear predictor (eta) instead.}

\item{link}{A \link{resistance_link} object (default \code{\link[=link_exp]{link_exp()}}).}
}
\value{
A single-layer \link[terra:SpatRaster]{terra::SpatRaster} named \code{"resistance"} or
\code{"log_resistance"}.
}
\description{
Computes the resistance raster by applying a link function to the
linear predictor \eqn{\eta(x) = r_0 + \sum_{k} z_k \phi_k(x)}.
The default exponential link gives
\eqn{R(x) = \text{clamp}(\exp(\eta), R_{\min}, R_{\max})}.
Supply a different \link{resistance_link} to change the mapping.
}
\examples{
\dontrun{
  R <- create_resistance_surface(
    params = c(r_0 = 3, z_1 = -0.5, z_2 = 0.5),
    basis_stack = basis
  )
  # With a different link:
  R2 <- create_resistance_surface(
    params = c(r_0 = 3, z_1 = -0.5, z_2 = 0.5),
    basis_stack = basis, link = link_softplus()
  )
}
}
