% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resistance.R
\name{create_resistance_surface}
\alias{create_resistance_surface}
\title{Create a resistance surface from parameters and basis functions}
\usage{
create_resistance_surface(
  params,
  basis_stack,
  R_min = 1,
  R_max = 5000,
  return_log = FALSE
)
}
\arguments{
\item{params}{Named numeric vector \strong{or} list with elements
\verb{r_0, z_1, z_2, ..., z_K} where \emph{K} = number of basis layers.
The first element is the intercept; remaining elements are
coefficients for each basis function.}

\item{basis_stack}{A \link[terra:SpatRaster-class]{terra::SpatRaster} with \emph{K} layers (from
\code{\link[=create_basis_stack]{create_basis_stack()}}).}

\item{R_min, R_max}{Numeric.
Hard floor / ceiling applied after exponentiation (default 1 / 5000).}

\item{return_log}{If \code{TRUE}, return the log-resistance surface instead.}
}
\value{
A single-layer \link[terra:SpatRaster-class]{terra::SpatRaster} named \code{"resistance"} or
\code{"log_resistance"}.
}
\description{
Computes the resistance raster using an exponential (log) link:
\deqn{\log R(x) = r_0 + \sum_{k=1}^{K} z_k \phi_k(x)}{
  log R(x) = r0 + z1*phi1(x) + ... + zK*phiK(x)}
}
\examples{
\dontrun{
  R <- create_resistance_surface(
    params = c(r_0 = 3, z_1 = -0.5, z_2 = 0.5),
    basis_stack = basis
  )
}
}
