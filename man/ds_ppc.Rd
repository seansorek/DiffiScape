% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ds_ppc}
\alias{ds_ppc}
\title{Posterior predictive check}
\usage{
ds_ppc(
  posterior_samples,
  intensity_fit,
  obs_points,
  connectivity,
  intensity_config = default_intensity_config(),
  covariates_rasters = NULL,
  family = NULL,
  n_sim = 200L,
  test_quantities = c("total_count", "vmi_ratio", "mean_deviance"),
  include_moran = FALSE,
  thin = 1L,
  seed = NULL,
  plot = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{posterior_samples}{Data.frame from \code{\link[=posterior_sample]{posterior_sample()}} (one row
per draw, columns include intensity parameters such as \code{alpha},
\code{gamma}, \verb{beta_*}, and optionally \code{size}).}

\item{intensity_fit}{Result from \code{\link[=fit_intensity_nb]{fit_intensity_nb()}},
\code{\link[=evaluate_full_model]{evaluate_full_model()}}, or \code{\link[=ds_fit_intensity]{ds_fit_intensity()}}.  Supplies the
connectivity standardisation constants (\code{c_scale}, \code{log_conn_mean},
\code{log_conn_sd}).}

\item{obs_points}{Data.frame with \verb{x, y}.}

\item{connectivity}{A \link[terra:SpatRaster]{terra::SpatRaster} of connectivity values.}

\item{intensity_config}{Intensity config list.}

\item{covariates_rasters}{Named list of covariate rasters (or \code{NULL}).}

\item{family}{An \link{intensity_family} object, or \code{NULL} (defaults to NB).}

\item{n_sim}{Number of posterior draws to simulate from.}

\item{test_quantities}{Character vector of test quantity names.
Supported: \code{"total_count"}, \code{"vmi_ratio"}, \code{"mean_deviance"}.}

\item{include_moran}{Logical; also compute Moran's I per replicate
(slow; requires \code{spdep}).}

\item{thin}{Integer; use every \code{thin}-th row of \code{posterior_samples}.}

\item{seed}{Optional integer seed for reproducibility.}

\item{plot}{Logical; produce PPC diagnostic plots.}

\item{verbose}{Logical; emit progress messages.}
}
\value{
A list with class \code{"ds_ppc"}:
\describe{
\item{\code{observed}}{Named numeric vector of observed test quantities.}
\item{\code{simulated}}{Named list of numeric vectors (one per test
quantity, length \code{n_sim}).}
\item{\code{bayesian_p}}{Named numeric vector of two-sided Bayesian
p-values.}
\item{\code{n_sim}}{Integer; number of simulations performed.}
\item{\code{test_quantities}}{Character vector of test quantity names.}
}
}
\description{
Simulates replicate datasets from the posterior predictive distribution
and compares test quantities to the observed data.  Returns Bayesian
p-values for each test quantity.
}
\seealso{
\code{\link[=diagnose_model]{diagnose_model()}}, \code{\link[=posterior_sample]{posterior_sample()}}, \code{\link[=plot_ppc]{plot_ppc()}}
}
