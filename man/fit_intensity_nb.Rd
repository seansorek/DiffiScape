% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intensity.R
\name{fit_intensity_nb}
\alias{fit_intensity_nb}
\title{Fit the negative-binomial PPP intensity model}
\usage{
fit_intensity_nb(
  connectivity_at_obs,
  connectivity_raster,
  obs_coords,
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  config = default_intensity_config(),
  family = NULL,
  available_connectivity = NULL,
  available_covariates = NULL,
  response = NULL
)
}
\arguments{
\item{connectivity_at_obs}{Numeric vector of raw connectivity at
observation locations.}

\item{connectivity_raster}{A \link[terra:SpatRaster-class]{terra::SpatRaster} for integration.}

\item{obs_coords}{Data.frame / matrix with \verb{x, y}.}

\item{covariates_obs}{Named list of covariate vectors at observations
(each 0–1 scaled; \code{NULL} to omit).}

\item{covariates_rasters}{Named list of \link[terra:SpatRaster-class]{terra::SpatRaster} for
integration-grid covariates (\code{NULL} to omit).}

\item{residualise}{Logical; residualise connectivity against
covariates before fitting (default \code{FALSE}).}

\item{config}{List from \code{\link[=default_intensity_config]{default_intensity_config()}}.}

\item{family}{An \link{intensity_family} object.  If \code{NULL} (default),
uses \code{\link[=family_negbin]{family_negbin()}} for backward compatibility.}

\item{available_connectivity}{Numeric vector of raw connectivity at
available/background locations.  When non-\code{NULL}, bypasses raster
quadrature and uses these values with unit weights instead.}

\item{available_covariates}{Named list of covariate vectors at available
locations.  Used together with \code{available_connectivity}.}

\item{response}{Optional numeric vector of observed responses.
Required when \code{family} is \code{family_gaussian()}; ignored for count
families such as \code{family_negbin()} and \code{family_poisson()}.}
}
\value{
A list with \code{estimates}, \code{se}, \code{loglik}, \code{convergence},
\code{c_scale}, \code{log_conn_mean}, \code{log_conn_sd},
\code{residualisation_info}.
}
\description{
Inner-loop MLE for \code{alpha}, \code{gamma}, optional covariate betas, and NB
dispersion.  Uses \code{\link[stats:optim]{stats::optim()}} with \code{"L-BFGS-B"}.
}
