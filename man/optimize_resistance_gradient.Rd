% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer.R
\name{optimize_resistance_gradient}
\alias{optimize_resistance_gradient}
\title{Optimise resistance via JAX automatic differentiation}
\usage{
optimize_resistance_gradient(
  basis_stack,
  obs_points,
  bounds = NULL,
  config = default_optimizer_config(),
  intensity_config = default_intensity_config(),
  output_dir = tempdir(),
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  available_points = NULL,
  available_covariates = NULL,
  method = "lbfgs",
  parameterization = "resistance",
  model_type = "parametric",
  model_config = list(),
  optim_config = list()
)
}
\arguments{
\item{basis_stack}{A \link[terra:SpatRaster]{terra::SpatRaster} of basis functions.}

\item{obs_points}{Data.frame with \verb{x, y} columns.}

\item{bounds}{Named list of \code{c(lower, upper)} per parameter (or
\code{NULL} for defaults).}

\item{config}{Optimiser configuration (see \code{\link[=default_optimizer_config]{default_optimizer_config()}}).}

\item{intensity_config}{Intensity config (see \code{\link[=default_intensity_config]{default_intensity_config()}}).}

\item{output_dir}{Directory for logs and results.}

\item{covariates_obs}{Named list of covariate vectors at obs.}

\item{covariates_rasters}{Named list of \link[terra:SpatRaster]{terra::SpatRaster} covariates.}

\item{residualise}{Logical; residualise connectivity.}

\item{available_points}{Optional data.frame with \verb{x, y} columns of
available/background locations for selection function families.}

\item{available_covariates}{Named list of covariate vectors at
\code{available_points} locations.}

\item{method}{Character; \code{"lbfgs"} (default) or \code{"adam"}.
Ignored when \code{model_type} is not \code{"parametric"}.}

\item{parameterization}{Character; \code{"resistance"} (default) or
\code{"permeability"}.}

\item{model_type}{Character; \code{"parametric"} (default, existing
L-BFGS / Adam path), \code{"mlp"}, \code{"conv"}, \code{"spline_gam"}, or
\code{"irl"} (Flax neural-network resistance models).}

\item{model_config}{Named list of model-specific parameters
forwarded to the Flax module constructor (ignored for
\code{model_type = "parametric"}).}

\item{optim_config}{Named list with \code{lr}, \code{n_epochs}, \code{patience}
for the neural optimizer (ignored for \code{model_type = "parametric"}).}
}
\value{
A list with \code{best_params}, \code{best_loglik}, \code{bounds},
\code{n_evaluations}, \code{distribution}, \code{convergence}.
}
\description{
Uses L-BFGS (via jaxopt) or Adam (via optax) optimisation with
\code{jax.grad} through the JAXScape differentiable circuit solver.
This replaces the former Julia/Enzyme.jl optimizer.
}
\details{
When \code{model_type} is not \code{"parametric"}, dispatches to the Flax
neural-network optimizer (\code{\link[=ds_jax_neural_optimize]{ds_jax_neural_optimize()}}) instead of
the parametric optimizer.  Recognised neural model types are
\code{"mlp"}, \code{"conv"}, \code{"spline_gam"}, and \code{"irl"}.
}
