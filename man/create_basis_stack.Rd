% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis_functions.R
\name{create_basis_stack}
\alias{create_basis_stack}
\title{Create a basis-function stack from environmental rasters}
\usage{
create_basis_stack(rasters, rescale = TRUE, mask_to = NULL, cache_path = NULL)
}
\arguments{
\item{rasters}{A \strong{named} list of single-layer \code{SpatRaster} objects.
Names become layer names in the output stack (e.g.
\code{list(canopy = rast_canopy, impervious = rast_imp)}).}

\item{rescale}{Logical; if \code{TRUE} (default), each layer is linearly
rescaled so that its valid (non-NA) values span [0, 1].}

\item{mask_to}{Character name of the layer whose NA pattern should be
applied to \strong{all} layers (unified study-area mask).
\code{NULL} (default) skips masking.}

\item{cache_path}{Optional file path.
If the file exists it is loaded and returned immediately;
otherwise the new stack is written there for future re-use.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with one layer per input raster.
}
\description{
Accepts an arbitrary named list of \link[terra:SpatRaster-class]{terra::SpatRaster} layers, validates
alignment, optionally rescales each to [0, 1], and returns a multi-layer
\code{SpatRaster} ready for \code{\link[=create_resistance_surface]{create_resistance_surface()}}.
}
\details{
Any input raster whose geometry (CRS, extent, resolution) does not match
the \strong{first} raster is automatically resampled to it (bilinear for
continuous layers, nearest-neighbour for layers with only 0/1 values).
}
\examples{
\dontrun{
  canopy <- terra::rast("canopy.tif")
  imperv <- terra::rast("impervious.tif")
  basis  <- create_basis_stack(list(canopy = canopy, impervious = imperv))
}
}
