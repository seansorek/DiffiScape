% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jax_bridge.R
\name{ds_jax_sample_advi}
\alias{ds_jax_sample_advi}
\title{ADVI posterior sampling via NumPyro}
\usage{
ds_jax_sample_advi(
  basis_stack,
  obs_points,
  n_samples = 2000L,
  max_iter = 2000L,
  lr = 0.01,
  parameterization = "resistance",
  model_type = "mlp",
  model_config = list(),
  seed = 42L,
  verbose = TRUE,
  output_dir = NULL
)
}
\arguments{
\item{basis_stack}{A \link[terra:SpatRaster]{terra::SpatRaster} with K covariate layers.}

\item{obs_points}{Data.frame with \verb{x, y} columns (projected coords).}

\item{n_samples}{Integer; number of posterior draws (default 2000).}

\item{max_iter}{Integer; maximum SVI iterations (default 2000).}

\item{lr}{Numeric; Adam learning rate for the ELBO (default 0.01).}

\item{parameterization}{Character; \code{"resistance"} (default) or
\code{"permeability"}.}

\item{model_type}{Character; Flax model architecture -- \code{"mlp"} (default),
\code{"conv"}, \code{"spline_gam"}, or \code{"irl"}.}

\item{model_config}{Named list of model-specific knobs forwarded to the
Flax module constructor (e.g., \code{list(hidden_dim = 32L)}).}

\item{seed}{Integer; random seed (default 42).}

\item{verbose}{Logical; print progress messages (default \code{TRUE}).}

\item{output_dir}{Character; directory for saving MCMC artifacts.
Defaults to \code{tempdir()}.}
}
\value{
A list with:
\describe{
\item{samples_effective_loglinear}{Matrix of posterior samples (or
\code{NULL} if unavailable).}
\item{samples_alpha}{\code{NULL} (placeholder).}
\item{samples_gamma}{\code{NULL} (placeholder).}
\item{partial_effects}{\code{NULL} (placeholder).}
\item{log_posterior_trace}{\code{NULL} (placeholder).}
\item{ess}{\code{NULL} (placeholder).}
\item{summary}{Per-parameter posterior summaries.}
\item{elapsed_time}{Numeric; wall-clock seconds.}
\item{n_divergences}{Integer 0 (ADVI has no divergences).}
\item{best_elbo}{Numeric; best (final) ELBO estimate.}
\item{converged}{Logical; whether the ELBO stabilised.}
}
}
\description{
Fits a low-rank multivariate normal variational posterior using NumPyro's
Stochastic Variational Inference (SVI) with Trace ELBO, then draws
posterior samples from the fitted guide.
}
