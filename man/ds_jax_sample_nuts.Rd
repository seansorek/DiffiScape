% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jax_bridge.R
\name{ds_jax_sample_nuts}
\alias{ds_jax_sample_nuts}
\title{NUTS posterior sampling via NumPyro}
\usage{
ds_jax_sample_nuts(
  basis_stack,
  obs_points,
  n_samples = 1000L,
  warmup = 1000L,
  max_treedepth = 10L,
  target_accept = 0.8,
  parameterization = "resistance",
  model_type = "mlp",
  model_config = list(),
  seed = 42L,
  verbose = TRUE,
  output_dir = NULL
)
}
\arguments{
\item{basis_stack}{A \link[terra:SpatRaster]{terra::SpatRaster} with K covariate layers.}

\item{obs_points}{Data.frame with \verb{x, y} columns (projected coords).}

\item{n_samples}{Integer; number of posterior draws (default 1000).}

\item{warmup}{Integer; warm-up / adaptation steps (default 1000).}

\item{max_treedepth}{Integer; NUTS maximum tree depth (default 10).}

\item{target_accept}{Numeric; target acceptance probability (default 0.80).}

\item{parameterization}{Character; \code{"resistance"} (default) or
\code{"permeability"}.}

\item{model_type}{Character; Flax model architecture -- \code{"mlp"} (default),
\code{"conv"}, \code{"spline_gam"}, or \code{"irl"}.}

\item{model_config}{Named list of model-specific knobs forwarded to the
Flax module constructor (e.g., \code{list(hidden_dim = 32L)}).}

\item{seed}{Integer; random seed (default 42).}

\item{verbose}{Logical; print progress messages (default \code{TRUE}).}

\item{output_dir}{Character; directory for saving MCMC artifacts.
Defaults to \code{tempdir()}.}
}
\value{
A list with:
\describe{
\item{samples_effective_loglinear}{Matrix of posterior samples (or
\code{NULL} if unavailable from the sampler output).}
\item{samples_alpha}{\code{NULL} (placeholder for compatibility).}
\item{samples_gamma}{\code{NULL} (placeholder for compatibility).}
\item{partial_effects}{\code{NULL} (placeholder for compatibility).}
\item{log_posterior_trace}{\code{NULL} (placeholder for compatibility).}
\item{ess}{\code{NULL} (placeholder for compatibility).}
\item{summary}{Per-parameter posterior summaries.}
\item{elapsed_time}{Numeric; wall-clock seconds.}
\item{n_divergences}{Integer; number of divergent transitions.}
}
}
\description{
Draws posterior samples for the parameters of a Flax neural-network
resistance model using the No-U-Turn Sampler implemented in NumPyro.
A \code{Normal(0, 2)} prior is placed on each (flattened) model parameter, and
the likelihood chains through JAXScape circuit-theory resistance distances.
}
