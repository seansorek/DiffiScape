% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intensity_family.R
\name{family_clogit}
\alias{family_clogit}
\title{Conditional logistic selection family (iSSA / SSA)}
\usage{
family_clogit(stratum_ids_used = NULL, stratum_ids_avail = NULL)
}
\arguments{
\item{stratum_ids_used}{Integer or character vector of length \eqn{n_\text{used}},
giving the stratum each used location belongs to.  Must be provided
together with \code{stratum_ids_avail}.}

\item{stratum_ids_avail}{Integer or character vector of length
\eqn{n_\text{avail}}, giving the stratum each available location belongs
to.}
}
\value{
An \link{intensity_family} object.
}
\description{
Conditional logistic regression for paired used-available data, optionally
stratified (one used location per stratum, any number of available).
When strata are omitted, reduces to a global \code{\link[=family_rsf]{family_rsf()}}.
}
\details{
Requires explicitly-provided available locations via the \code{available_points}
argument of \code{\link[=optimize_resistance]{optimize_resistance()}}, \code{\link[=ds_optimize]{ds_optimize()}}, or
\code{\link[=fit_intensity_selection]{fit_intensity_selection()}}.

For stratified data (e.g. step-selection analysis), pass stratum IDs at
family creation time and set
\code{intensity_config$integration_subsample = 1} to preserve stratum integrity.
}
