% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jax_bridge.R
\name{ds_jax_optimize}
\alias{ds_jax_optimize}
\title{Run gradient-based parametric optimisation via the JAX backend}
\usage{
ds_jax_optimize(
  basis_np,
  obs_np,
  valid_mask_np,
  n_rows,
  n_cols,
  cell_area,
  init_params = NULL,
  ...
)
}
\arguments{
\item{basis_np}{Numpy array of basis values (n_valid_cells x n_basis).}

\item{obs_np}{Numpy array of observation counts per valid cell.}

\item{valid_mask_np}{Numpy boolean mask of shape (n_rows * n_cols,).}

\item{n_rows, n_cols}{Integer grid dimensions.}

\item{cell_area}{Numeric cell area in square map units.}

\item{init_params}{Numeric vector of initial parameters (intercept +
basis coefficients).  If \code{NULL}, defaults to zeros.}

\item{...}{Additional arguments forwarded to the Python function
(e.g. \code{method}, \code{lr}, \code{n_epochs}, \code{patience}, \code{parameterization},
\code{link_fn}, \code{radius}, \code{block_size}, \code{seed}, \code{verbose},
\code{lower_bounds}/\code{upper_bounds} -- per-resistance-parameter box
constraints, same length as \code{init_params}; enforced via \code{LBFGSB} or
per-step clipping, not just used to pick the starting point).}
}
\value{
A list (converted from the Python dict) with \code{best_params},
\code{best_loglik}, \code{loss_history}, \code{n_epochs_run}, \code{elapsed}, \code{converged}.
}
\description{
Calls \code{diffiscape_jax.optimize.run_parametric_optimization}, which
minimises the negative log-likelihood of a connectivity-based
point-process model using L-BFGS or Adam with JAX auto-diff gradients.
}
\details{
The optimize module is imported lazily and cached in \code{.jax_env$optimize_module}.
}
\keyword{internal}
