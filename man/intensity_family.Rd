% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intensity_family.R
\name{intensity_family}
\alias{intensity_family}
\title{Create an intensity family object}
\usage{
intensity_family(
  name,
  negloglik_fn,
  deviance_residuals_fn,
  init_fn,
  n_extra_params = 0L,
  extra_param_names = character(0),
  param_names_fn = NULL
)
}
\arguments{
\item{name}{Character label (e.g. \code{"negbin"}, \code{"poisson"}).}

\item{negloglik_fn}{Function implementing the PPP negative
log-likelihood.  Signature:
\verb{(theta, z_obs, z_int, int_weights, obs_weights, cov_obs, cov_int, cov_names)}
returning a scalar.}

\item{deviance_residuals_fn}{Function computing per-cell deviance
residuals.  Signature: \verb{(observed, fitted, extra_params)} returning
a numeric vector.}

\item{init_fn}{Function \code{(n_cov)} returning a list with elements
\code{start}, \code{lower}, \code{upper} — numeric vectors of starting values and
bounds for \code{\link[stats:optim]{stats::optim()}}.}

\item{n_extra_params}{Integer; number of distribution-specific
parameters beyond \verb{alpha, gamma, betas} (e.g. 1 for NB's theta).}

\item{extra_param_names}{Character vector naming the extra parameters.}

\item{param_names_fn}{Optional function \code{(cov_names)} returning a
character vector of parameter names for families with non-standard
layouts (e.g., no \code{alpha}).  \code{NULL} uses the default
\code{alpha}/\code{gamma}/\verb{beta_*} naming.}
}
\value{
An S3 object of class \code{"intensity_family"}.
}
\description{
An \code{intensity_family} encapsulates the negative log-likelihood,
deviance residual formula, and starting-value logic for a specific
distributional family used in the point-process intensity model.
}
