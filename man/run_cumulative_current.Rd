% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connectivity.R
\name{run_cumulative_current}
\alias{run_cumulative_current}
\title{Compute cumulative current (and optionally voltage) using the JAX
differentiable solver}
\usage{
run_cumulative_current(
  resistance,
  radius = 13L,
  block_size = 5L,
  output = "current",
  parameterization = "resistance"
)
}
\arguments{
\item{resistance}{A single-layer \link[terra:SpatRaster]{terra::SpatRaster} of resistance.}

\item{radius}{Integer; moving-window radius in pixels (default 13).}

\item{block_size}{Integer; source-block side length (default 5).}

\item{output}{Character; which quantity to return from the circuit solve.
One of \code{"current"} (default), \code{"voltage"}, or \code{"both"}.
\itemize{
\item \code{"current"} returns only cumulative current density.
\item \code{"voltage"} returns only cumulative voltage (flow potential).
\item \code{"both"} returns both.
}}

\item{parameterization}{Character; \code{"resistance"} (default) or
\code{"permeability"}.}
}
\value{
A list with:
\describe{
\item{cum_current}{\link[terra:SpatRaster]{terra::SpatRaster} of cumulative current, or \code{NULL}
when \code{output = "voltage"}.}
\item{flow_potential}{\link[terra:SpatRaster]{terra::SpatRaster} of cumulative voltage
(flow potential), or \code{NULL} when \code{output = "current"}.}
\item{elapsed_seconds}{Wall-clock time.}
}
}
\description{
In-memory solver that requires no file I/O.  Delegates to
\code{\link[=ds_jax_connectivity]{ds_jax_connectivity()}} which uses JAXScape's conjugate-gradient
circuit solver.
}
