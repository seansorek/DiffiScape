% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intensity.R
\name{.nb_negloglik_cached}
\alias{.nb_negloglik_cached}
\title{Negative log-likelihood for the NB PPP intensity model (cached version)}
\usage{
.nb_negloglik_cached(
  theta,
  z_obs,
  z_int,
  int_weights,
  obs_weights,
  cov_obs = NULL,
  cov_int = NULL,
  cov_names = character(0)
)
}
\arguments{
\item{theta}{Parameter vector.  Layout: \verb{c(alpha, gamma, [betas...], log_nb_theta)}.  The last element is the log-dispersion parameter
(\code{size} in \link[stats:dnbinom]{stats::dnbinom}).}

\item{z_obs, z_int}{Standardised connectivity at observations /
integration points.}

\item{int_weights}{Quadrature weights for integration points.}

\item{obs_weights}{Observation weights (typically all 1).}

\item{cov_obs, cov_int}{Named lists of covariate vectors (or \code{NULL}).}

\item{cov_names}{Character vector of covariate names (determines
which elements of \code{theta} map to betas).}
}
\value{
Scalar negative log-likelihood.
}
\description{
Operates on pre-extracted numeric vectors to avoid repeated raster I/O
inside the optimiser.
}
\keyword{internal}
