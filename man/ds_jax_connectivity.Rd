% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jax_bridge.R
\name{ds_jax_connectivity}
\alias{ds_jax_connectivity}
\title{Compute cumulative current via the JAX backend}
\usage{
ds_jax_connectivity(
  resistance,
  radius = 13L,
  block_size = 5L,
  source_from_resistance = TRUE,
  parameterization = "resistance",
  output = "current"
)
}
\arguments{
\item{resistance}{A single-layer \link[terra:SpatRaster]{terra::SpatRaster} of resistance.}

\item{radius}{Integer; moving-window radius (default 13).}

\item{block_size}{Integer; source block side length (default 5).}

\item{source_from_resistance}{Logical (default \code{TRUE}).}

\item{parameterization}{Character; \code{"resistance"} (default) or
\code{"permeability"}.}

\item{output}{Character; \code{"current"}, \code{"voltage"}, or \code{"both"}.}
}
\value{
A list matching the \code{\link[=run_cumulative_current]{run_cumulative_current()}} interface:
\describe{
\item{cum_current}{A \link[terra:SpatRaster]{terra::SpatRaster} of cumulative current, or
\code{NULL} if \code{output = "voltage"}.}
\item{flow_potential}{A \link[terra:SpatRaster]{terra::SpatRaster} of flow potential, or
\code{NULL} if \code{output = "current"}.}
\item{elapsed_seconds}{Numeric; wall-clock seconds.}
}
}
\description{
Core connectivity solver that uses JAXScape's differentiable circuit
solver via the JAX backend.
}
