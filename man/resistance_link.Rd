% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resistance_link.R
\name{resistance_link}
\alias{resistance_link}
\title{Create a resistance link object}
\usage{
resistance_link(
  name,
  forward_fn,
  inverse_fn,
  deriv_fn,
  needs_clamp = TRUE,
  eta_fn = NULL
)
}
\arguments{
\item{name}{Character label (e.g. \code{"exp"}, \code{"softplus"}).}

\item{forward_fn}{Function \verb{(eta, R_min, R_max) -> R}.}

\item{inverse_fn}{Function \verb{(R, R_min, R_max) -> eta}.}

\item{deriv_fn}{Function \verb{(eta, R_min, R_max) -> dR/deta}.}

\item{needs_clamp}{Logical; does the link require external clamping
to \verb{[R_min, R_max]}?  Links that are naturally bounded (e.g.
softplus) can set this to \code{FALSE}.}

\item{eta_fn}{Optional function \verb{(theta, basis_values) -> eta}
overriding the default additive linear predictor.  Use this for
non-additive models (tensor products, neural nets, etc.).}
}
\value{
An S3 object of class \code{"resistance_link"}.
}
\description{
A \code{resistance_link} encapsulates the mapping between the linear
predictor \eqn{\eta(x)} and the resistance surface \eqn{R(x)}, plus
the derivative needed for gradient-based optimisation.
}
