% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{diffiscape}
\alias{diffiscape}
\title{Run the full DiffiScape pipeline}
\usage{
diffiscape(
  obs_data,
  rasters,
  julia_home = NULL,
  optimizer_config = default_optimizer_config(),
  intensity_config = default_intensity_config(),
  bounds = NULL,
  output_dir = "diffiscape_output",
  n_posterior = 200L,
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  plot = TRUE,
  crs = NULL,
  rescale_basis = TRUE,
  pattern = "*.tif",
  solver = c("surrogate", "enzyme")
)
}
\arguments{
\item{obs_data}{Either a file path (CSV / shapefile) or a data.frame
with \verb{x, y} columns.}

\item{rasters}{Either a directory, file paths, or a named list of
\link[terra:SpatRaster-class]{terra::SpatRaster} objects.}

\item{julia_home}{Path to the Julia binary (or \code{NULL} for PATH).}

\item{optimizer_config}{Config list (see \code{\link[=default_optimizer_config]{default_optimizer_config()}}).}

\item{intensity_config}{Config list (see \code{\link[=default_intensity_config]{default_intensity_config()}}).}

\item{bounds}{Parameter bounds (or \code{NULL} for defaults).}

\item{output_dir}{Directory for all outputs.}

\item{n_posterior}{Number of posterior draws (\code{0} to skip).}

\item{covariates_obs}{Named list of covariate vectors.}

\item{covariates_rasters}{Named list of covariate rasters.}

\item{residualise}{Logical.}

\item{plot}{Logical; produce diagnostic plots.}

\item{crs}{Target CRS for reprojection (if input is spatial file).}

\item{rescale_basis}{Logical; rescale basis rasters.}

\item{pattern}{File pattern for raster directory.}

\item{solver}{Character; \code{"surrogate"} (default, GP surrogate optimiser) or
\code{"enzyme"} (L-BFGS via differentiable Julia solver).}
}
\value{
A list with \code{obs_points}, \code{basis_stack}, \code{opt_result},
\code{intensity_fit}, \code{posterior}, \code{diagnostics}.
}
\description{
A single high-level function that chains every step of the pipeline:
data loading, basis creation, Julia start-up, optimisation, intensity
fitting, posterior inference, and diagnostics.
}
