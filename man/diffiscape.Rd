% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{diffiscape}
\alias{diffiscape}
\title{Run the full DiffiScape pipeline}
\usage{
diffiscape(
  obs_data,
  rasters,
  optimizer_config = default_optimizer_config(),
  intensity_config = default_intensity_config(),
  bounds = NULL,
  output_dir = "diffiscape_output",
  n_posterior = 200L,
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  available_points = NULL,
  available_covariates = NULL,
  plot = TRUE,
  crs = NULL,
  rescale_basis = TRUE,
  pattern = "*.tif",
  omniscape_settings = list(),
  solver = c("surrogate", "gradient", "enzyme")
)
}
\arguments{
\item{obs_data}{Either a file path (CSV / shapefile) or a data.frame
with \verb{x, y} columns.}

\item{rasters}{Either a directory, file paths, or a named list of
\link[terra:SpatRaster]{terra::SpatRaster} objects.}

\item{optimizer_config}{Config list (see \code{\link[=default_optimizer_config]{default_optimizer_config()}}).}

\item{intensity_config}{Config list (see \code{\link[=default_intensity_config]{default_intensity_config()}}).}

\item{bounds}{Parameter bounds (or \code{NULL} for defaults).}

\item{output_dir}{Directory for all outputs.}

\item{n_posterior}{Number of posterior draws (\code{0} to skip).}

\item{covariates_obs}{Named list of covariate vectors.}

\item{covariates_rasters}{Named list of covariate rasters.}

\item{residualise}{Logical.}

\item{available_points}{Optional data.frame with \verb{x, y} columns of
available/background locations for selection function families.  When
supplied, bypasses raster quadrature and uses these locations with unit
weights instead.  \code{NULL} (default) uses standard area-weighted raster
integration.}

\item{available_covariates}{Named list of covariate vectors at
\code{available_points} locations.  Required when \code{available_points} is
supplied and the intensity model includes covariates.}

\item{plot}{Logical; produce diagnostic plots.}

\item{crs}{Target CRS for reprojection (if input is spatial file).}

\item{rescale_basis}{Logical; rescale basis rasters.}

\item{pattern}{File pattern for raster directory.}

\item{omniscape_settings}{Named list of Omniscape overrides forwarded to
every connectivity step (final refit, posterior sampling, and
diagnostics).  Recognised entries: \code{radius} (default \code{13L}),
\code{block_size} (default \code{5L}), \code{cleanup} (default \code{TRUE}).}

\item{solver}{Character; \code{"surrogate"} (default, GP surrogate optimiser),
\code{"gradient"} (L-BFGS / Adam via JAX auto-diff), or \code{"enzyme"} (deprecated,
alias for \code{"gradient"}).}
}
\value{
A list with \code{obs_points}, \code{basis_stack}, \code{opt_result},
\code{intensity_fit}, \code{posterior}, \code{diagnostics}.
}
\description{
A single high-level function that chains every step of the pipeline:
data loading, basis creation, optimisation, intensity fitting,
posterior inference, and diagnostics.
}
