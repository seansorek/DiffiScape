% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{ds_optimize}
\alias{ds_optimize}
\title{Optimise resistance parameters}
\usage{
ds_optimize(
  basis_stack,
  obs_points,
  bounds = NULL,
  config = default_optimizer_config(),
  intensity_config = default_intensity_config(),
  output_dir = tempdir(),
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  available_points = NULL,
  available_covariates = NULL,
  solver = c("surrogate", "gradient", "enzyme", "torch", "irl"),
  model_type = "parametric"
)
}
\arguments{
\item{basis_stack}{Basis function stack.}

\item{obs_points}{Data.frame with \verb{x, y}.}

\item{bounds}{Optional parameter bounds.}

\item{config}{Optimiser config (see \code{\link[=default_optimizer_config]{default_optimizer_config()}}).
For \code{solver = "torch"}, may contain a \code{torch} sublist whose entries
are forwarded to \code{\link[=run_torch_pipeline]{run_torch_pipeline()}}.
For \code{solver = "gradient"} with a neural \code{model_type}, may contain
\code{model_config} and \code{optim_config} sublists.}

\item{intensity_config}{Intensity config (see \code{\link[=default_intensity_config]{default_intensity_config()}}).}

\item{output_dir}{Directory for logs.}

\item{covariates_obs}{Named list of covariate vectors.}

\item{covariates_rasters}{Named list of covariate rasters.}

\item{residualise}{Logical.}

\item{available_points}{Optional data.frame with \verb{x, y} columns of
available/background locations for selection function families.  When
supplied, bypasses raster quadrature and uses these locations with unit
weights instead.  \code{NULL} (default) uses standard area-weighted raster
integration.}

\item{available_covariates}{Named list of covariate vectors at
\code{available_points} locations.  Required when \code{available_points} is
supplied and the intensity model includes covariates.}

\item{solver}{Character; \code{"surrogate"} (GP + Thompson Sampling or
Expected Improvement, default), \code{"gradient"} (L-BFGS / Adam via JAX
auto-diff), \code{"enzyme"} (deprecated, alias for \code{"gradient"}),
\code{"torch"} (PyTorch neural-network resistance), or \code{"irl"}
(PyTorch value-shaped resistance: a reward network is turned into a
resistance surface via soft value iteration, then run through the same
differentiable circuit solver -- a convenience alias for \code{solver = "torch"}
with \code{model_type = "irl"}).}

\item{model_type}{Character; \code{"parametric"} (default), \code{"mlp"},
\code{"conv"}, \code{"spline_gam"}, or \code{"irl"}.  When not \code{"parametric"},
the gradient solver dispatches to the Flax neural-network
optimizer instead of the parametric L-BFGS/Adam path.}
}
\value{
Result from \code{\link[=optimize_resistance]{optimize_resistance()}},
\code{\link[=optimize_resistance_gradient]{optimize_resistance_gradient()}}, or \code{\link[=run_torch_pipeline]{run_torch_pipeline()}}.
}
\description{
Wrapper that dispatches to \code{\link[=optimize_resistance]{optimize_resistance()}} (GP surrogate),
\code{\link[=optimize_resistance_gradient]{optimize_resistance_gradient()}} (L-BFGS / Adam via JAX auto-diff,
or Flax neural-network resistance when \code{model_type} is set), or
\code{\link[=run_torch_pipeline]{run_torch_pipeline()}} (PyTorch neural-network resistance), depending
on \code{solver}.
}
\section{Config conventions}{

The three backend families are deliberately \emph{not} unified onto a common
config shape -- each keeps its pre-existing convention, and dispatch
(via an internal \code{solver_spec()} S3 mechanism) is the one place that
knows how to extract the right sublist for its solver:
\describe{
\item{\code{solver = "surrogate"}}{Plain top-level \code{config} entries (see
\code{\link[=default_optimizer_config]{default_optimizer_config()}}). No sublist.}
\item{\code{solver = "gradient"} / \code{"enzyme"}}{Top-level \code{config} entries
for the parametric L-BFGS/Adam path, plus optional
\code{config$model_config} / \code{config$optim_config} sublists when
\code{model_type} selects a neural resistance model.}
\item{\code{solver = "torch"} / \code{"irl"}}{A \code{config$torch} sublist forwarded
as named arguments to \code{\link[=run_torch_pipeline]{run_torch_pipeline()}}.}
}
}

