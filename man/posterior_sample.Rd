% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{posterior_sample}
\alias{posterior_sample}
\title{Joint posterior via Monte Carlo composition sampling}
\usage{
posterior_sample(
  laplace,
  opt_result,
  basis_stack,
  obs_points,
  n_draws = 200L,
  n_inner = 5L,
  bounds = NULL,
  omniscape_settings = list(),
  intensity_config = default_intensity_config(),
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  available_points = NULL,
  available_covariates = NULL,
  link = link_exp(),
  family = NULL,
  seed = 42L
)
}
\arguments{
\item{laplace}{Result from \code{\link[=laplace_resistance]{laplace_resistance()}}.}

\item{opt_result}{Result from \code{\link[=optimize_resistance]{optimize_resistance()}}.}

\item{basis_stack}{A \link[terra:SpatRaster-class]{terra::SpatRaster} of basis functions.}

\item{obs_points}{Data.frame with \verb{x, y}.}

\item{n_draws}{Number of posterior draws.}

\item{n_inner}{Number of inner-loop draws per outer draw.}

\item{bounds}{Named list of parameter bounds.}

\item{omniscape_settings}{List with connectivity settings.}

\item{intensity_config}{List from \code{\link[=default_intensity_config]{default_intensity_config()}}.}

\item{covariates_obs}{Named list of covariate vectors.}

\item{covariates_rasters}{Named list of covariate rasters.}

\item{residualise}{Logical.}

\item{available_points}{Optional data.frame with \verb{x, y} columns of
available/background locations for selection function families.  When
supplied, bypasses raster quadrature and uses these locations with unit
weights instead.  \code{NULL} (default) uses standard area-weighted raster
integration.}

\item{available_covariates}{Named list of covariate vectors at
\code{available_points} locations.  Required when \code{available_points} is
supplied and the intensity model includes covariates.}

\item{link}{A \link{resistance_link} object (default \code{\link[=link_exp]{link_exp()}}).}

\item{family}{An \link{intensity_family} object, or \code{NULL}.}

\item{seed}{Random seed.}
}
\value{
A data.frame with posterior samples (one row per draw).
}
\description{
For each draw of resistance parameters from the Laplace posterior,
evaluates the inner-loop MLE and draws intensity parameters from
the asymptotic normal.
}
