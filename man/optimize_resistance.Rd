% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer.R
\name{optimize_resistance}
\alias{optimize_resistance}
\title{Optimise resistance parameters via GP surrogate + Thompson Sampling}
\usage{
optimize_resistance(
  basis_stack,
  obs_points,
  bounds = NULL,
  config = default_optimizer_config(),
  intensity_config = default_intensity_config(),
  output_dir = tempdir(),
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  available_points = NULL,
  available_covariates = NULL
)
}
\arguments{
\item{basis_stack}{A \link[terra:SpatRaster]{terra::SpatRaster} of basis functions.}

\item{obs_points}{Data.frame with \verb{x, y} columns of observed locations.}

\item{bounds}{Named list of \code{c(lower, upper)} per parameter (see
\code{\link[=get_default_bounds]{get_default_bounds()}}).  \code{NULL} uses defaults.}

\item{config}{List from \code{\link[=default_optimizer_config]{default_optimizer_config()}}.}

\item{intensity_config}{List from \code{\link[=default_intensity_config]{default_intensity_config()}}.}

\item{output_dir}{Directory for logs and results.}

\item{covariates_obs}{Named list of covariate vectors at obs.}

\item{covariates_rasters}{Named list of \link[terra:SpatRaster]{terra::SpatRaster} covariates.}

\item{residualise}{Logical; residualise connectivity.}

\item{available_points}{Optional data.frame with \verb{x, y} columns of
available/background locations for selection function families.  When
supplied, bypasses raster quadrature and uses these locations with unit
weights instead.  \code{NULL} (default) uses standard area-weighted raster
integration.}

\item{available_covariates}{Named list of covariate vectors at
\code{available_points} locations.  Required when \code{available_points} is
supplied and the intensity model includes covariates.}
}
\value{
A list with \code{best_params}, \code{best_loglik}, \code{X_evaluated},
\code{y_evaluated}, \code{surrogate}, \code{bounds}, \code{n_evaluations}.
}
\description{
Two-stage loop:
\enumerate{
\item \strong{Outer}: Latin Hypercube initial design, then GP surrogate with
Thompson Sampling acquisition.
\item \strong{Inner}: MLE for intensity parameters given each connectivity
surface.
}
}
