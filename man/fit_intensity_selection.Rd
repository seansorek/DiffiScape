% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intensity.R
\name{fit_intensity_selection}
\alias{fit_intensity_selection}
\title{Fit a selection function intensity model}
\usage{
fit_intensity_selection(
  connectivity_at_obs,
  available_connectivity,
  obs_coords,
  available_covariates = NULL,
  covariates_obs = NULL,
  config = default_intensity_config(),
  family = family_rsf(),
  response = NULL
)
}
\arguments{
\item{connectivity_at_obs}{Numeric vector of raw connectivity at
used/presence locations.}

\item{available_connectivity}{Numeric vector of raw connectivity at
available/background locations.}

\item{obs_coords}{Data.frame / matrix with \verb{x, y} for used locations.}

\item{available_covariates}{Named list of covariate vectors at available
locations (\code{NULL} to omit).}

\item{covariates_obs}{Named list of covariate vectors at used locations
(\code{NULL} to omit).}

\item{config}{List from \code{\link[=default_intensity_config]{default_intensity_config()}}.}

\item{family}{An \link{intensity_family} object (default \code{\link[=family_rsf]{family_rsf()}}).}

\item{response}{Optional numeric vector of observed responses.
Required when \code{family} is \code{family_gaussian()}; ignored for count
families such as \code{family_negbin()} and \code{family_poisson()}.}
}
\value{
A list matching the interface of \code{\link[=fit_intensity_nb]{fit_intensity_nb()}}.
}
\description{
Convenience wrapper around \code{\link[=fit_intensity_nb]{fit_intensity_nb()}} for selection function
(RSF / RSP / iSSA) models.  Accepts explicit available/background locations
instead of a connectivity raster for quadrature.
}
