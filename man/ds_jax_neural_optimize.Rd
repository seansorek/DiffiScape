% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jax_bridge.R
\name{ds_jax_neural_optimize}
\alias{ds_jax_neural_optimize}
\title{Run neural-network resistance optimisation via the JAX backend}
\usage{
ds_jax_neural_optimize(
  basis_np,
  obs_np,
  valid_mask_np,
  n_rows,
  n_cols,
  cell_area,
  model_type = "mlp",
  model_config = list(),
  optim_config = list(),
  ...
)
}
\arguments{
\item{basis_np}{Numpy array of basis / covariate values.}

\item{obs_np}{Numpy array of observation counts per valid cell.}

\item{valid_mask_np}{Numpy boolean mask of shape (n_rows * n_cols,).}

\item{n_rows, n_cols}{Integer grid dimensions.}

\item{cell_area}{Numeric cell area in square map units.}

\item{model_type}{Character; \code{"mlp"} (default), \code{"conv"},
\code{"spline_gam"}, or \code{"irl"}.}

\item{model_config}{Named list of model-specific parameters forwarded
to the Flax module constructor.}

\item{optim_config}{Named list with \code{lr}, \code{n_epochs}, \code{patience}.}

\item{...}{Additional arguments forwarded to the Python function
(e.g. \code{parameterization}, \code{radius}, \code{block_size}, \code{seed}, \code{verbose}).
\code{radius}/\code{block_size} are honored: they parameterize the same
moving-window \code{window.cumulative_current()}-based solve used on the
forward/evaluation path, so the neural loss trains against the same
connectivity definition it is later evaluated against (GH #105).}
}
\value{
A list (converted from the Python dict) with \code{resistance},
\code{best_loglik}, \code{loss_history}, \code{n_epochs_run}, \code{elapsed}, \code{model_type}.
}
\description{
Calls \code{diffiscape_jax.optimize.run_neural_optimization}, which
trains a Flax neural network (MLP, Conv, Spline-GAM, or IRL)
to produce a resistance surface that maximises the connectivity-based
point-process log-likelihood.
}
\details{
The optimize module is imported lazily and cached in \code{.jax_env$optimize_module}.
}
\keyword{internal}
