% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer.R
\name{evaluate_full_model}
\alias{evaluate_full_model}
\title{Evaluate the full model for one set of resistance parameters}
\usage{
evaluate_full_model(
  resistance_params,
  basis_stack,
  obs_points,
  distribution = "negbin",
  omniscape_settings = list(),
  intensity_config = default_intensity_config(),
  covariates_obs = NULL,
  covariates_rasters = NULL,
  residualise = FALSE,
  verbose = TRUE,
  link = link_exp(),
  family = NULL,
  available_points = NULL,
  available_covariates = NULL
)
}
\arguments{
\item{resistance_params}{Resistance parameter vector or list.}

\item{basis_stack}{A \link[terra:SpatRaster]{terra::SpatRaster} of basis functions.}

\item{obs_points}{Data.frame with \verb{x, y} columns.}

\item{distribution}{\code{"negbin"} or \code{"gam"}.}

\item{omniscape_settings}{List with \code{radius}, \code{block_size}, \code{cleanup}.}

\item{intensity_config}{List from \code{\link[=default_intensity_config]{default_intensity_config()}}.}

\item{covariates_obs}{Named list of covariate vectors at obs.}

\item{covariates_rasters}{Named list of \link[terra:SpatRaster]{terra::SpatRaster} covariates.}

\item{residualise}{Logical.}

\item{verbose}{Logical.}

\item{link}{A \link{resistance_link} object (default \code{\link[=link_exp]{link_exp()}}).}

\item{family}{An \link{intensity_family} object, or \code{NULL} to use the
default for the chosen \code{distribution}.}

\item{available_points}{Optional data.frame with \verb{x, y} columns of
available/background locations for selection function families.  When
supplied, bypasses raster quadrature and uses these locations with unit
weights instead.  \code{NULL} (default) uses standard area-weighted raster
integration.}

\item{available_covariates}{Named list of covariate vectors at
\code{available_points} locations.  Required when \code{available_points} is
supplied and the intensity model includes covariates.}
}
\value{
A list with \code{loglik}, \code{intensity_params}, \code{intensity_se},
\code{hessian}, \code{total_time}, \code{convergence}, \code{distribution}.
}
\description{
Creates a resistance surface, runs connectivity computation, and fits
the inner-loop intensity model.
}
